import SetupClient from "@/components/admin/SetupClient";

export const metadata = { title: "Initial Setup", robots: { index: false } };

export default function SetupPage() {
  return <SetupClient />;
}
